{
  "name": "scheduling_app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reach/dialog": "^0.2.9",
    "@reach/tabs": "^0.1.6",
    "@reach/visually-hidden": "^0.1.4",
    "aviator": "^0.6.6",
    "netlify-cli": "^2.13.1",
    "netlify-lambda": "^1.6.3",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-netlify-identity": "^0.1.9",
    "react-netlify-identity-widget": "^0.1.4",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lambdas:build": "npx netlify-lambda build src/lambdas",
    "lambdas:start": "npx netlify-lambda serve src/lambdas"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:9000",
  "devDependencies": {
    "@types/jest": "^24.0.18"
  }
}
